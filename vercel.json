{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": "views/**"
      }
    }
  ],
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
